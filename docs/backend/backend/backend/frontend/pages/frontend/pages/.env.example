# Database URL (example)
DATABASE_URL="postgresql://username:password@localhost:5432/x_like_db?schema=public"

# JWT secret key sample - must be changed in real env
JWT_SECRET="replace_this_with_a_secure_random_value"
